const array = [];
let text = '';
if(array.length === 0){
    text = '배열이 비어있습니다.';
} else{
    text = '배열이 비어있지 않습니다';
}

console.log(text);

const array2 = [];
        //배열이 0이        =  참이니(true)           :       거짓이지(False)
let tt = array.length === 0 ? '배열이 비어있습니다.' : '배열이 비어있지 않습니다.';
console.log(tt);

const condition1 = false;
const condition2 = false;
